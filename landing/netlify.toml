[build]
  command = "bun run build"
  publish = "build"

[build.environment]
  BUN_VERSION = "1.3.14"

# Handle fallback routing for SPA redirects if needed
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
